package twinspire;

import twinspire.render.GraphicsContext;
import twinspire.render.UpdateContext;
import twinspire.render.TrackingObject;

class IdAssoc {
    
    /**
    * A collection of tracking objects associated to an Id. To create
    * a tracking object, use `true` as an argument when creating an `Id`.
    **/
    public static var assoc:Array<TrackingObject>;

}